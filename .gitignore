# Dependências do Node
/node_modules

# Arquivos de log
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variáveis de ambiente
.env
.env.development
.env.production
.env.local

# Informações de sistema operacional
.DS_Store
Thumbs.db

# Ignorar o próprio arquivo .gitignore se ele fosse auto-gerado, o que não é o caso aqui.
# .gitignore

# Arquivos de cobertura de teste
/coverage

# Build artifacts
/build
/dist

# npm package-lock.json (se você preferir que ele seja gerado por cada desenvolvedor)
# package-lock.json

# yarn.lock (se você preferir que ele seja gerado por cada desenvolvedor)
# yarn.lock